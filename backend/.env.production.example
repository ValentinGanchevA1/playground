# =============================================================================
# G88 Backend - Production Environment Variables TEMPLATE
# =============================================================================
# 
# IMPORTANT: This is a TEMPLATE file. DO NOT store real credentials here.
# Copy to .env.production and fill in values, but NEVER commit real secrets.
#
# For Render deployment, set these in the Render Dashboard only.
# =============================================================================

# Server
PORT=3001
NODE_ENV=production

# Database (Auto-configured by Render)
DATABASE_HOST=auto_configured_by_render
DATABASE_PORT=5432
DATABASE_USER=auto_configured_by_render
DATABASE_PASSWORD=auto_configured_by_render
DATABASE_NAME=g88db_production
DATABASE_URL=auto_configured_by_render

# Redis (Auto-configured by Render)
REDIS_HOST=auto_configured_by_render
REDIS_PORT=6379
REDIS_URL=auto_configured_by_render

# JWT (Auto-generated by Render)
JWT_SECRET=auto_generated_by_render
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=auto_generated_by_render
JWT_REFRESH_EXPIRES_IN=7d

# Stripe - SET IN RENDER DASHBOARD ONLY
STRIPE_SECRET_KEY=sk_live_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Twilio - SET IN RENDER DASHBOARD ONLY
TWILIO_ACCOUNT_SID=ACxxx
TWILIO_AUTH_TOKEN=xxx
TWILIO_PHONE_NUMBER=+xxx

# SendGrid - SET IN RENDER DASHBOARD ONLY
SENDGRID_API_KEY=SG.xxx

# AWS S3 - SET IN RENDER DASHBOARD ONLY
AWS_REGION=eu-north-1
AWS_ACCESS_KEY_ID=xxx
AWS_SECRET_ACCESS_KEY=xxx
AWS_S3_BUCKET=g88-uploads-production

# Google OAuth - SET IN RENDER DASHBOARD ONLY
GOOGLE_CLIENT_ID=xxx
GOOGLE_CLIENT_SECRET=xxx

# Face Recognition
AWS_REKOGNITION_COLLECTION_ID=g88-faces-production
